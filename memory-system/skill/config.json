{
  "agent_support": {
    "claude": {"min_version": "3.0"},
    "roo": {"min_version": "1.0"},
    "cascade": {"min_version": "1.0"},
    "generic": {"requirements": ["file_access", "file_write"]}
  },
  "triggers": {
    "keywords": [
      "memory system",
      "event log",
      "event sourced",
      "changelog",
      "knowledge graph",
      "agent memory",
      "agent state",
      ".memory folder",
      "context regeneration",
      "append only log"
    ],
    "patterns": [
      "set.*up.*memory.*system",
      "deploy.*memory.*system",
      "create.*memory.*folder",
      "initialize.*changelog",
      "initialize.*event.*log",
      "add.*memory.*system",
      "implement.*memory.*system",
      "agent.*state.*tracking",
      "regenerate.*context"
    ]
  },
  "requirements": {
    "tools": [],
    "permissions": ["file_read", "file_write"],
    "notes": "Language-agnostic skill. Pure markdown — no runtime, no JSON, no tooling required. Works with any project type."
  },
  "examples": {
    "simple": [
      "Set up a memory system for this project",
      "Initialize the .memory/ folder",
      "Regenerate context.md — it's stale"
    ],
    "complex": [
      "Deploy a full-tier memory system with knowledge graph and dual-file pattern for this monorepo",
      "Archive old events from changelog.md — it's over 50 events",
      "Audit the memory system — check consistency across all layers"
    ]
  }
}
