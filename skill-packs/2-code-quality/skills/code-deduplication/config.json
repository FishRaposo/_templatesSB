{
  "agent_support": {
    "claude": {"min_version": "3.0"},
    "roo": {"min_version": "1.0"},
    "cascade": {"min_version": "1.0"},
    "generic": {"requirements": ["file_access", "code_execution"]}
  },
  "triggers": {
    "keywords": [
      "duplicate code",
      "duplication",
      "DRY",
      "copy paste",
      "repeated code",
      "redundant",
      "consolidate",
      "shared utility"
    ],
    "patterns": [
      "remove.*duplicate",
      "eliminate.*duplication",
      "same.*code.*multiple",
      "extract.*shared",
      "consolidate.*logic",
      "DRY.*principle"
    ]
  },
  "requirements": {
    "tools": [],
    "permissions": ["file_read", "file_write"],
    "notes": "Language-agnostic skill. Works with any programming language."
  },
  "examples": {
    "simple": [
      "Find duplicate code in this project",
      "These two functions are almost identical, consolidate them",
      "Extract shared logic from these files"
    ],
    "complex": [
      "Analyze the codebase for duplication and create a deduplication plan",
      "Consolidate validation logic scattered across 5 services",
      "Create shared utilities from repeated patterns in this monorepo"
    ]
  }
}
