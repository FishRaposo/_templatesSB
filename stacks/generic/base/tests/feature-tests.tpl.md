# Feature Tests

Include at least these tokens somewhere in your feature tests for each feature id:

- feature_<id>__happy_path
- feature_<id>__validation (or feature_<id>__permission)
- feature_<id>__failure (only if failure_modes are defined)
