# Feature Tests (R)

For each feature id in docs/FEATURES.yaml, include at least:

- `feature_<id>__happy_path`
- `feature_<id>__validation` (or `feature_<id>__permission`)
- `feature_<id>__failure` (if `failure_modes` defined)
