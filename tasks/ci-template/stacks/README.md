# CI Template Stack Implementations

This task uses stack-agnostic templates since CI/CD workflows apply across all stacks.

## Available Implementations

- `agnostic/` - Stack-agnostic CI templates (GitHub Actions, GitLab CI, etc.)

## Usage

CI templates are automatically selected based on the target platform, not the application stack.
